# 現在地マップ（迷子防止用）

最終更新: 2026-06-25

このファイルは「どこに何があるか分からない」を防ぐための、コード用語ナシの地図です。

---

## いちばん大事なこと

**あなたが編集する場所は、たった1つ。**

> 🟢 `src/app/b-plan-v2/page.tsx` ＝ 新メインページ（Bプラン V2）

- これが今「トップ（/）」として表示されます（2026-06-25 接続済み）。
- URL は `/` でも `/b-plan-v2` でも、同じこのページが出ます。
- 中身は「1枚の長いページ」。セクション（不安・実績・価格・声・支払い例…）が全部この1ファイルに入っています。

---

## ページの全体像（今あるもの）

### 🟢 メイン（あなたが育てる）
- **`/`（＝ b-plan-v2）** … 新メイン。編集はここだけ。

### 🔵 メインが「続きはこちら」で送る詳細ページ（生かす）
- **`/money`** … 資金計画の詳細（メインの「費用」「支払い例」リンク先）
- **`/voice`** … お客様の声の詳細（メインの「声」リンク先）
- **`/reserve`** … 来場予約（メインのCTAリンク先 ×3）

### ⚪️ 旧A案の残りページ（今はそのまま置いてある／未整理）
- `/works`（施工事例・GSAPめくり）、`/lots`、`/staff`、`/standard`、`/sell`、`/refer`、`/contact`、`/sitemap`、`/privacy`
- ※ `/works` は **新メインからまだリンクされていません**（繋ぐかどうかは保留）。
- ※ 施工事例の写真は、メイン(b-plan-v2)の中にも別途内蔵されています（`/works` ページとは別実装）。

### 📦 アーカイブ済み（消さず別フォルダへ退避・いつでも戻せる）
場所: プロジェクト直下 `_archive_旧A案_20260625/`
- `b-plan`（v1・v2と紛らわしかった重複版）
- `font-lab` / `voice-lab` / `standard-equipment-lab`（実験用ラボ）
- `_snap_*.mjs`（一時スクリプト）

---

## まだ片付けられる（安全・任意）

- `screenshots/`（開発用スクショ 約111MB）… 動作に無関係。退避候補。
- 旧トップA案の部品 約40個（`src/components/sections/` 内）… 今はどこからも使われていない旧ヒーロー実験など。退避候補。
- 旧A案ページ（`/lots` `/staff` 等）の扱い … 今は動くので保留。新メインに残すか決めてから。

---

## 困ったときの合言葉

- 「編集する場所は？」→ **`src/app/b-plan-v2/page.tsx` だけ**
- 「消しちゃった？」→ **消してない。`_archive_旧A案_20260625/` か Git 履歴に全部ある**
- 整理の続きは Claude にまかせてOK。ファイルの場所は覚えなくて大丈夫。
</content>
</invoke>
